/*
    USER Actions
*/

export const USER_ACTIONS = {
    LOGIN_USER: 'LOGIN_USER',
    LOGOUT_USER: 'LOGOFF_USER'
}

export const login = (u,p) => (dispatch) => {
    // Extract the page name from path.
};

export const logout = (u,p) => (dispatch) => {
    // Extract the page name from path.
};